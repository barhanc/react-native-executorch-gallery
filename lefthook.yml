pre-commit:
  parallel: true
  commands:
    types:
      glob: '*.{ts,tsx}'
      run: npx tsc --noEmit
    format:
      glob: '*.{ts,tsx,json,md}'
      run: npx prettier --write {staged_files} && git add {staged_files}
